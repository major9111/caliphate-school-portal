"""Storage service (Supabase or local fallback).""" import os import uuid from typing import Optional from fastapi import UploadFile from app.core.config import settings class StorageService: """Uploads files to Supabase Storage, or falls back to local disk.""" def __init__(self): self.use_supabase = bool(settings.SUPABASE_URL and settings.SUPABASE_KEY) if self.use_supabase: try: from supabase import create_client self.client = create_client(settings.SUPABASE_URL, settings.SUPABASE_KEY) except Exception: self.use_supabase = False self.local_dir = os.path.join(os.getcwd(), "uploads") os.makedirs(self.local_dir, exist_ok=True) async def upload(self, file: UploadFile, folder: str = "general") -> str: ext = os.path.splitext(file.filename or "file")[1] filename = f"{uuid.uuid4().hex}{ext}" path = f"{folder}/{filename}" if self.use_supabase: content = await file.read() self.client.storage.from_(settings.SUPABASE_BUCKET).upload(path, content, {"content-type": file.content_type or "application/octet-stream"}) public = self.client.storage.from_(settings.SUPABASE_BUCKET).get_public_url(path) return public # Local fallback full_path = os.path.join(self.local_dir, folder) os.makedirs(full_path, exist_ok=True) with open(os.path.join(full_path, filename), "wb") as f: content = await file.read() f.write(content) return f"/uploads/{path}" async def delete(self, url: str) -> None: if self.use_supabase and settings.SUPABASE_BUCKET in url: try: path = url.split(f"/{settings.SUPABASE_BUCKET}/")[-1] self.client.storage.from_(settings.SUPABASE_BUCKET).remove([path]) except Exception: pass 