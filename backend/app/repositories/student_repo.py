"""Student repository.""" from typing import Optional, List from sqlalchemy.orm import Session, joinedload from app.models.student import Student, Guardian from app.repositories.base import BaseRepository class StudentRepository(BaseRepository[Student]): def __init__(self, db: Session): super().__init__(Student, db) def get_with_guardian(self, id: int) -> Optional[Student]: return ( self.db.query(Student).options(joinedload(Student.guardian)).filter(Student.id == id).first() ) def get_by_admission_number(self, admission_number: str) -> Optional[Student]: return self.db.query(Student).filter(Student.admission_number == admission_number).first() def get_many_with_guardian(self, skip: int = 0, limit: int = 20, search: Optional[str] = None) -> List[Student]: q = self.db.query(Student).options(joinedload(Student.guardian)) if search: like = f"%{search}%" q = q.filter( (Student.first_name.ilike(like)) | (Student.last_name.ilike(like)) | (Student.admission_number.ilike(like)) ) return q.offset(skip).limit(limit).all() def count_by_gender(self) -> dict: from sqlalchemy import func rows = self.db.query(Student.gender, func.count(Student.id)).group_by(Student.gender).all() return {g: c for g, c in rows} def count_active(self) -> int: return self.db.query(Student).filter(Student.is_active == 1).count() class GuardianRepository(BaseRepository[Guardian]): def __init__(self, db: Session): super().__init__(Guardian, db) 