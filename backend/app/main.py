"""Main FastAPI application entry point.""" import logging from fastapi import FastAPI from fastapi.middleware.cors import CORSMiddleware from app.core.database import engine, Base from app.api.v1.router import router as api_router from app.core.security_headers import SecurityHeadersMiddleware # Configure logging logging.basicConfig(level=logging.INFO) logger = logging.getLogger(__name__) # Create database tables Base.metadata.create_all(bind=engine) app = FastAPI( title="Caliphate International Schools API", description="Enterprise School Management System API", version="2.0.0" ) # Security Headers Middleware app.add_middleware(SecurityHeadersMiddleware) # CORS Middleware app.add_middleware( CORSMiddleware, allow_origins=["http://localhost:5173", "http://localhost:3000"], allow_credentials=True, allow_methods=["*"], allow_headers=["*"], ) # Include API routes app.include_router(api_router, prefix="/api/v1") @app.on_event("startup") async def startup_event(): logger.info(" Starting Caliphate International Schools API v2.0.0") logger.info(" Security headers enabled") logger.info(" Audit trail active") logger.info(" RBAC system initialized") @app.get("/") def root(): return {"status": "healthy", "system": "Caliphate Schools API"} 