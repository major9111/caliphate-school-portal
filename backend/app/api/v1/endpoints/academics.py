"""Academic endpoints: sessions, terms, classes, subjects, timetables.""" from typing import List from fastapi import APIRouter, Depends, HTTPException from sqlalchemy.orm import Session from app.core.database import get_db from app.core.dependencies import require_admin_or_above from app.repositories.academic_repo import SessionRepository, TermRepository, ClassRepository, SubjectRepository from app.models.academic import AcademicSession, AcademicTerm, ClassLevel, Subject, Timetable from app.schemas.academic import ( AcademicSessionCreate, AcademicSessionOut, AcademicTermCreate, AcademicTermOut, ClassLevelCreate, ClassLevelOut, SubjectCreate, SubjectOut, TimetableCreate, TimetableOut, ) from app.models.user import User router = APIRouter() # Sessions @router.get("/sessions", response_model=List[AcademicSessionOut]) def list_sessions(db: Session = Depends(get_db)): return SessionRepository(db).get_many(limit=100) @router.post("/sessions", response_model=AcademicSessionOut, status_code=201) def create_session(payload: AcademicSessionCreate, db: Session = Depends(get_db), _: User = Depends(require_admin_or_above)): session = AcademicSession(**payload.model_dump()) return SessionRepository(db).create(session) @router.get("/sessions/current") def current_session(db: Session = Depends(get_db)): s = SessionRepository(db).get_current() if not s: raise HTTPException(404, "No current session") return s # Terms @router.get("/terms", response_model=List[AcademicTermOut]) def list_terms(db: Session = Depends(get_db)): return TermRepository(db).get_many(limit=100) @router.post("/terms", response_model=AcademicTermOut, status_code=201) def create_term(payload: AcademicTermCreate, db: Session = Depends(get_db), _: User = Depends(require_admin_or_above)): term = AcademicTerm(**payload.model_dump()) return TermRepository(db).create(term) # Classes @router.get("/classes", response_model=List[ClassLevelOut]) def list_classes(db: Session = Depends(get_db)): return ClassRepository(db).get_many(limit=100) @router.post("/classes", response_model=ClassLevelOut, status_code=201) def create_class(payload: ClassLevelCreate, db: Session = Depends(get_db), _: User = Depends(require_admin_or_above)): cls = ClassLevel(**payload.model_dump()) return ClassRepository(db).create(cls) # Subjects @router.get("/subjects", response_model=List[SubjectOut]) def list_subjects(db: Session = Depends(get_db)): return SubjectRepository(db).get_many(limit=200) @router.post("/subjects", response_model=SubjectOut, status_code=201) def create_subject(payload: SubjectCreate, db: Session = Depends(get_db), _: User = Depends(require_admin_or_above)): subject = Subject(**payload.model_dump()) return SubjectRepository(db).create(subject) # Timetable @router.get("/timetable", response_model=List[TimetableOut]) def list_timetable(class_id: int = None, term_id: int = None, db: Session = Depends(get_db)): q = db.query(Timetable) if class_id: q = q.filter(Timetable.class_id == class_id) if term_id: q = q.filter(Timetable.term_id == term_id) return q.all() @router.post("/timetable", response_model=TimetableOut, status_code=201) def create_timetable(payload: TimetableCreate, db: Session = Depends(get_db), _: User = Depends(require_admin_or_above)): tt = Timetable(**payload.model_dump()) db.add(tt) db.commit() db.refresh(tt) return tt 