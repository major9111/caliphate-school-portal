"""Reports endpoints (PDF/Excel export).""" from io import BytesIO from fastapi import APIRouter, Depends from fastapi.responses import StreamingResponse from sqlalchemy.orm import Session from app.core.database import get_db from app.core.dependencies import require_admin_or_above from app.models.student import Student from app.models.user import User from app.models.user import User as UserModel router = APIRouter() @router.get("/students/csv") def export_students_csv(db: Session = Depends(get_db), _: User = Depends(require_admin_or_above)): import csv students = db.query(Student).all() output = BytesIO() writer = csv.writer(output) writer.writerow(["Admission No", "First Name", "Last Name", "Gender", "Class", "Status"]) for s in students: writer.writerow([s.admission_number, s.first_name, s.last_name, s.gender, "", s.admission_status]) output.seek(0) return StreamingResponse( iter([output.getvalue()]), media_type="text/csv", headers={"Content-Disposition": "attachment; filename=students.csv"}, ) @router.get("/students/excel") def export_students_excel(db: Session = Depends(get_db), _: User = Depends(require_admin_or_above)): import openpyxl students = db.query(Student).all() wb = openpyxl.Workbook() ws = wb.active ws.title = "Students" ws.append(["Admission No", "First Name", "Last Name", "Gender", "Status"]) for s in students: ws.append([s.admission_number, s.first_name, s.last_name, s.gender, s.admission_status]) output = BytesIO() wb.save(output) output.seek(0) return StreamingResponse( iter([output.getvalue()]), media_type="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet", headers={"Content-Disposition": "attachment; filename=students.xlsx"}, ) 