"""Bulk import/export and reports.""" from typing import Optional from fastapi import APIRouter, Depends, UploadFile, File, Query from fastapi.responses import StreamingResponse from sqlalchemy.orm import Session from io import BytesIO from app.core.database import get_db from app.core.dependencies import require_admin_or_above, require_teacher_or_above from app.services.part2a.bulk_service import BulkService from app.services.part2a.report_service import ReportService from app.models.user import User router = APIRouter() @router.get("/students/export") def export_students( class_level_id: Optional[int] = None, db: Session = Depends(get_db), _: User = Depends(require_admin_or_above), ): service = BulkService(db) csv_bytes = service.export_students_csv(class_level_id) return StreamingResponse( BytesIO(csv_bytes), media_type="text/csv", headers={"Content-Disposition": "attachment; filename=students.csv"}, ) @router.post("/students/import") async def import_students( file: UploadFile = File(...), db: Session = Depends(get_db), current_user: User = Depends(require_admin_or_above), ): content = await file.read() service = BulkService(db) return service.import_students_csv(content.decode("utf-8"), current_user.id) @router.get("/reports/admission-register") def admission_register(session_id: int, status: Optional[str] = None, db: Session = Depends(get_db), _: User = Depends(require_teacher_or_above)): return ReportService(db).admission_register(session_id, status) @router.get("/reports/student-register") def student_register(class_level_id: Optional[int] = None, db: Session = Depends(get_db), _: User = Depends(require_teacher_or_above)): return ReportService(db).student_register(class_level_id) @router.get("/reports/gender-statistics") def gender_stats(db: Session = Depends(get_db)): return ReportService(db).gender_statistics() @router.get("/reports/age-distribution") def age_distribution(class_level_id: Optional[int] = None, db: Session = Depends(get_db)): return ReportService(db).age_distribution(class_level_id) @router.get("/reports/enrollment-summary") def enrollment_summary(db: Session = Depends(get_db)): return ReportService(db).enrollment_summary() @router.get("/reports/promotions") def promotion_report(session_id: int, db: Session = Depends(get_db)): return ReportService(db).promotion_report(session_id) @router.get("/reports/transfers") def transfer_report(year: int, db: Session = Depends(get_db)): return ReportService(db).transfer_report(year) 