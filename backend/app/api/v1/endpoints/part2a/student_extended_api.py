"""Extended student endpoints: documents, medical alerts, profile.""" from typing import Optional from fastapi import APIRouter, Depends, HTTPException, UploadFile, File, Form from sqlalchemy.orm import Session from app.core.database import get_db from app.core.dependencies import require_teacher_or_above, get_current_active_user from app.services.part2a.student_extended_service import StudentExtendedService from app.services.storage_service import StorageService from app.models.user import User router = APIRouter() @router.get("/{student_id}/profile") def get_profile(student_id: int, db: Session = Depends(get_db), _: User = Depends(require_teacher_or_above)): service = StudentExtendedService(db) try: return service.get_profile_summary(student_id) except ValueError as e: raise HTTPException(404, str(e)) @router.get("/{student_id}/documents") def list_documents(student_id: int, db: Session = Depends(get_db), _: User = Depends(require_teacher_or_above)): service = StudentExtendedService(db) return service.list_documents(student_id) @router.post("/{student_id}/documents") async def upload_document( student_id: int, document_type: str = Form(...), file: UploadFile = File(...), title: Optional[str] = Form(None), description: Optional[str] = Form(None), db: Session = Depends(get_db), current_user: User = Depends(require_teacher_or_above), ): service = StudentExtendedService(db) storage = StorageService() url = await storage.upload(file, folder=f"students/{student_id}") return service.add_document( student_id=student_id, document_type=document_type, file_url=url, thumbnail_url=None, file_name=file.filename or "file", file_size=file.size or 0, mime_type=file.content_type or "application/octet-stream", title=title, description=description, uploaded_by_id=current_user.id, ) @router.delete("/documents/{doc_id}") def delete_document(doc_id: int, db: Session = Depends(get_db), current_user: User = Depends(require_teacher_or_above)): service = StudentExtendedService(db) if not service.delete_document(doc_id, current_user.id): raise HTTPException(404, "Document not found") return {"message": "Deleted"} @router.get("/{student_id}/medical-alerts") def list_alerts(student_id: int, db: Session = Depends(get_db), _: User = Depends(require_teacher_or_above)): service = StudentExtendedService(db) return service.list_alerts(student_id) @router.post("/{student_id}/medical-alerts") def add_alert( student_id: int, alert_type: str = Form(...), title: str = Form(...), details: Optional[str] = Form(None), severity: str = Form("normal"), db: Session = Depends(get_db), current_user: User = Depends(require_teacher_or_above), ): service = StudentExtendedService(db) return service.add_alert(student_id, alert_type, title, details, severity, current_user.id) @router.delete("/medical-alerts/{alert_id}") def deactivate_alert(alert_id: int, db: Session = Depends(get_db), _: User = Depends(require_teacher_or_above)): service = StudentExtendedService(db) if not service.deactivate_alert(alert_id): raise HTTPException(404, "Alert not found") return {"message": "Deactivated"} @router.get("/search") def search_students(q: str, db: Session = Depends(get_db), _: User = Depends(require_teacher_or_above)): service = StudentExtendedService(db) return service.search(q) 