"""Enhanced attendance models with approval workflow.""" from sqlalchemy import ( Column, Integer, String, Text, Boolean, Date, DateTime, ForeignKey, JSON, Index, ) from app.core.database import Base from app.models.base import TimestampMixin class AttendanceRegister(TimestampMixin, Base): """Daily attendance register (one per class per day).""" __tablename__ = "attendance_registers" id = Column(Integer, primary_key=True, index=True) class_level_id = Column(Integer, ForeignKey("class_levels.id"), nullable=False) arm_id = Column(Integer, ForeignKey("class_arms_v2.id"), nullable=True) session_id = Column(Integer, ForeignKey("academic_sessions.id"), nullable=False) term_id = Column(Integer, ForeignKey("academic_terms.id"), nullable=False) date = Column(Date, nullable=False, index=True) period = Column(String(20), default="morning") # morning, afternoon, full_day status = Column(String(20), default="draft") # draft, submitted, approved, corrected marked_by_id = Column(Integer, ForeignKey("users.id"), nullable=False) approved_by_id = Column(Integer, ForeignKey("users.id"), nullable=True) approved_at = Column(DateTime(timezone=True), nullable=True) total_students = Column(Integer, default=0) present_count = Column(Integer, default=0) absent_count = Column(Integer, default=0) late_count = Column(Integer, default=0) excused_count = Column(Integer, default=0) notes = Column(Text, nullable=True) __table_args__ = ( Index("ix_register_unique", "class_level_id", "arm_id", "date", "period", unique=True), Index("ix_register_date", "date", "status"), ) class AttendanceRecord(TimestampMixin, Base): """Individual attendance record per student per register.""" __tablename__ = "attendance_records" id = Column(Integer, primary_key=True, index=True) register_id = Column(Integer, ForeignKey("attendance_registers.id", ondelete="CASCADE"), nullable=False, index=True) student_id = Column(Integer, ForeignKey("students.id", ondelete="CASCADE"), nullable=False, index=True) status = Column(String(20), nullable=False) # present, absent, late, excused check_in_time = Column(String(10), nullable=True) check_out_time = Column(String(10), nullable=True) remark = Column(Text, nullable=True) is_corrected = Column(Boolean, default=False) correction_reason = Column(Text, nullable=True) corrected_by_id = Column(Integer, ForeignKey("users.id"), nullable=True) __table_args__ = ( Index("ix_record_register_student", "register_id", "student_id", unique=True), ) 