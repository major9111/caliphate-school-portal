"""Simple In-Memory Cache with TTL for high performance.""" import time from typing import Any, Dict, Optional class Cache: def __init__(self): self._store: Dict[str, Dict[str, Any]] = {} def get(self, key: str) -> Optional[Any]: """Get item from cache if it exists and hasn't expired.""" if key in self._store: item = self._store[key] if time.time() < item['expires_at']: return item['value'] else: # Expired del self._store[key] return None def set(self, key: str, value: Any, ttl: int = 60): """Set item in cache with Time-To-Live (seconds).""" self._store[key] = { 'value': value, 'expires_at': time.time() + ttl } def delete(self, key: str): """Remove item from cache.""" if key in self._store: del self._store[key] def clear(self): """Clear entire cache.""" self._store.clear() # Global cache instance cache = Cache() 